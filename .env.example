JWT_SECRET=replace-with-at-least-48-random-bytes
PORT=5500
ALLOWED_ORIGIN=http://localhost:3000

# Production Postgres — set DATABASE_URL to use a connection string instead of
# individual host/user/password fields (update the Pool config in index.js).
# DATABASE_URL=postgres://user:password@host:5432/dbname
